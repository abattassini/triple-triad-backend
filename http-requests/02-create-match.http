
# Environment Variables
@baseUrl = http://localhost:5041
@apiPath = /api/game
@contentType = application/json
@accept = application/json
@testPlayer1 = alice
@testPlayer2 = bob
@aiPlayer = AI

### Create New Match
# Creates a new match. The creator is taken from the JWT (no playerId in body).
POST {{baseUrl}}{{apiPath}}/match
Content-Type: {{contentType}}
Accept: {{accept}}
{{authHeader}}

{
  "opponentId": "{{testPlayer2}}"
}

###

### Create Match vs AI
# Creates a new match against AI
POST {{baseUrl}}{{apiPath}}/match
Content-Type: {{contentType}}
Accept: {{accept}}
{{authHeader}}

{
  "opponentId": "{{aiPlayer}}"
}

###
